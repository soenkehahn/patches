name: patches-client

executables:
  patches-client:
    main: Main.hs
    source-dirs:
      - ./.
      - ../src
    dependencies:
      - base
      - reflex-dom
      - string-conversions
      - servant >= 0.5
